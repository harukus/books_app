<%= form_for(@comment, url: { controller: :comments, action: :update})do |form| %>
<div class="field">
    <%= form.label :content, "コメント" %>
    <%= form.text_area :content %>
</div>
<div class="actions">
    <%= form.submit %>
</div>
<% end %>
